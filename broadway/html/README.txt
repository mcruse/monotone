Files in this directory are for the Broadway.core package and the get copied
to properties.HTTP_ROOT and properties.HTTPS_ROOT directories, during install.  

All html files for other packages should be placed in that package directory, NOT here.