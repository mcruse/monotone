#===================================================
# build directories
#===================================================
src.flex.main.dir=src/main/flex
bin.flex.debug.dir=bin-debug
bin.flex.release.dir=bin-release

#====================================================
# Version #
#====================================================
com.cisco.app.core.version=1.0

#====================================================
# The prefix for the swc.
# This value will have -debug-[version#].swc added
#====================================================
library.name.prefix=core.cisco.app.core

PROJECT_DIR=./
SOURCE_DIR=${PROJECT_DIR}/src/main/flex
OUTPUT_DIR=${PROJECT_DIR}/bin-release
